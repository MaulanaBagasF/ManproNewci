<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Tarif Mesin Cuci - UKM Newci</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: #f0f4f8;
            display: flex;
            min-height: 100vh;
            color: #2c3e50;
        }

        .sidebar {
            width: 250px;
            background-color: #2c3e50;
            padding-top: 30px;
        }

        .sidebar ul {
            list-style-type: none;
        }

        .sidebar ul li a {
            display: block;
            color: #ecf0f1;
            padding: 15px 20px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .sidebar ul li a:hover {
            background-color: #34495e;
        }

        .main-content {
            flex-grow: 1;
            padding: 30px;
            background-color: #f0f4f8;
        }

        .card {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            padding: 30px;
            max-width: 500px;
            margin: 0 auto;
        }

        .card-header {
            text-align: center;
            margin-bottom: 25px;
            color: #2980b9;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #2c3e50;
            font-weight: 500;
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            border: 1px solid #bdc3c7;
            border-radius: 6px;
            background-color: #f8f9fa;
            font-size: 16px;
        }

        .form-group input:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 2px rgba(52, 152, 219, 0.2);
        }

        .btn {
            width: 100%;
            padding: 12px;
            background-color: #2980b9;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            font-weight: 600;
        }

        .btn:hover {
            background-color: #3498db;
        }

        .readonly-input {
            background-color: #e9ecef;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <ul>
            <li><a href="/dashboard">Dashboard</a></li>
            <li><a href="/kelola-mesin-cuci">Kelola Mesin Cuci</a></li>
            <li><a href="/kelola-pelanggan">Kelola Pelanggan</a></li>
            <li><a href="/laporan">Laporan Pendapatan</a></li>
        </ul>
    </div>
    
    <div class="main-content">
        <div class="card">
            <div class="card-header">
                <h2>Edit Tarif Mesin Cuci</h2>
            </div>
            <form id="edit-mesin-cuci-form" action="/edit-mesin-cuci/<%= mesin.nama %>" method="POST">
                <div class="form-group">
                    <label for="nama">Nama Mesin</label>
                    <input type="text" id="nama" name="nama" value="<%= mesin.nama %>" readonly class="readonly-input">
                </div>
                <div class="form-group">
                    <label for="merek">Merek</label>
                    <input type="text" id="merek" name="merek" value="<%= mesin.merek %>" readonly class="readonly-input">
                </div>
                <div class="form-group">
                    <label for="kapasitas">Kapasitas</label>
                    <input type="number" id="kapasitas" name="kapasitas" value="<%= mesin.kapasitas %>" readonly class="readonly-input">
                </div>
                <div class="form-group">
                    <label for="tarif">Tarif (per 15 menit)</label>
                    <input type="number" id="tarif" name="tarif" value="<%= mesin.tarif %>" required>
                </div>
                <button type="submit" class="btn">Simpan Perubahan</button>
            </form>
        </div>
    </div>
</body>
</html>
